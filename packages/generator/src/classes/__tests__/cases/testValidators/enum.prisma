generator client {
    provider = "prisma-client"
}

datasource db {
    provider = "postgresql"
}

model EnumValidators {
    id          Int  @id @default(autoincrement())
    enum        TestEnum? /// @zod.enum.describe('test')
    enumTwo     TestEnum ///@zod.custom.omit([input])
}

enum TestEnum {
    a
    b
}

