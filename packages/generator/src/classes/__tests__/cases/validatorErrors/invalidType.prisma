generator client {
    provider = "prisma-client"
}

datasource db {
    provider = "postgresql"
}

model MyModel {
    id     Int     @id @default(autoincrement())
    string String /// @zod.string.min(3)
    custom String? /// @zod.asdf.min(3)
}
